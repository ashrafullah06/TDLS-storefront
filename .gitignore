############################ 
# OS X
############################

.DS_Store
.AppleDouble
.LSOverride
Icon
.Spotlight-V100
.Trashes
._*


############################
# Linux
############################

*~


############################
# Windows
############################

Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msm
*.msp


############################
# Packages
############################

*.7z
*.csv
*.dat
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
*.com
*.class
*.dll
*.exe
*.o
*.seed
*.so
*.swo
*.swp
*.swn
*.swm
*.out
*.pid


############################
# Logs and databases
############################

.tmp
.tmp/
.cache/
*.log
*.sql
*.sqlite
*.sqlite3


############################
# Misc.
############################

*#
ssl
.idea
.vscode/
nbproject
.tsbuildinfo
.eslintcache

############################
# Node.js
############################

lib-cov
lcov.info
pids
logs
results
node_modules/
.node_history

############################
# Package managers
############################

.yarn/*
!.yarn/cache
!.yarn/unplugged
!.yarn/patches
!.yarn/releases
!.yarn/sdks
!.yarn/versions
!.yarn/versions/.gitkeep
.pnp.*
yarn-error.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log
yarn-error.log*

############################
# Tests
############################

coverage/

############################
# Next.js (.next project)
############################

.next/
out/
.vercel/
.turbo/

# TypeScript (keep next-env.d.ts if you want it committed; otherwise ignore)
# next-env.d.ts

# env files (do not commit secrets; allow template)
.env
.env.*
!.env.example

# local/backup files (explicitly ignore known backups + artifacts)
- Copy.env.bak
*.bak
*.env.bak
Untitled-*.js

# scratch/experiment homepage files (do not commit)
src/components/homepage/image within the container distorted.js
src/components/homepage/without blur, cropped image.js
src/components/homepage/with blur-homepage-client.jsx

# local/tool artifacts (never commit)
Set-Content
npm
npx
cross-env-shell
my-project@0.1.0

# Prisma backups (never commit)
prisma/**/schema.backup.prisma
*.backup.*

############################
# Prisma generated clients + engines
############################

prisma/generated/
**/query_engine-*.node

# --- IMPORTANT: keep generated Prisma clients tracked for Vercel builds ---
# Some other ignore rule(s) may be ignoring src/generated/prisma; this block forces it back in.
src/generated/*
!src/generated/prisma/
!src/generated/prisma/**
!src/generated/prisma/app/
!src/generated/prisma/app/**
!src/generated/prisma/strapi/
!src/generated/prisma/strapi/**

# --- IMPORTANT: do NOT commit Prisma platform engines inside src/generated ---
src/generated/**/query_engine-*.node
src/generated/**/libquery_engine-*.node
src/generated/**/schema-engine-*.node

############################
# Build outputs
############################

dist/
build/

# Prisma generated engines / binaries (do not commit)
src/generated/prisma/**/query-engine-*.exe
src/generated/prisma/**/runtime/binary.*
prisma/**/*.bak*


############################################################
# UPGRADE (ADDITIVE ONLY — NO EXISTING LINES REMOVED)
# Goal: stop secrets/env from being committed + handle filenames with spaces
############################################################

# --- Vercel / env files (extra safety; prevents GitHub push protection blocks) ---
vercel*.env
*.production.env
*.env.production
.env.local
.env.development
.env.test

# --- Cover common secret exports / copies ---
*.env.backup
*.env.save
*.env.secret
*.secret
*.secrets
*.key
*.pem
*.p12
*.pfx

# --- Fix scratch paths that contain spaces/commas (escaped forms) ---
src/components/homepage/image\ within\ the\ container\ distorted.js
src/components/homepage/without\ blur,\ cropped\ image.js

# --- Extra: OS / tooling artifacts that commonly leak in Windows dev ---
*.orig
*.rej
*.patch
Thumbs.db:encryptable
